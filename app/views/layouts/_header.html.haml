%header.navbar.navbar-inverse.navbar-fixed-top{:role => 'navigation'}
  .container
    .navbar-header
      %button{:type => 'button', :class => 'navbar-toggle', :data => {:toggle => 'collapse', :target => '.navbar-colapse'}}
        %span.sr-only= 'Toggle navigation'
        %span.icon-bar= ''
        %span.icon-bar= ''
        %span.icon-bar= ''
      = link_to 'Default App', root_path, id: 'logo', :class => 'navbar-brand'
    .collapse.navbar-collapse
      %ul.nav.navbar-nav.pull-right
        - if signed_in?
          %li= link_to '[Link]', '#'
          %li#fat-menu.dropdown
            %a{:href => '#', :class => 'dropdown-toggle', :data => {:toggle => 'dropdown'}}
              Account
              %b.caret
            %ul.dropdown-menu
              %li= link_to '[Link]', '#'
              %li= link_to '[Link]', '#'
              %li.divider
              %li= link_to 'Sign out', '#'
        - else
          %li= link_to 'Sign in', '#'